

function checkPrime(n){
    let factor = ""

    for(let i = 0 ; i<n ;i++){
        if(n%i==0){
            factor++;
        }
    }
  
    if(factor == 2){
        return true;
    } else {
        return false ; }
}